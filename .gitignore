# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?

# IDE / editor caches (we commit launch.json + tasks.json,
# but not per-machine state like Chrome debug profiles)
.idea/
.vscode/.chrome-profile/
*.swp
*.swo
*~

# Vercel CLI local state (links, env pulls, build cache)
.vercel/

# Dependencies (kept here in case we add any later)
node_modules/
.pnpm-store/

# Local env / secrets
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output (none today, but future-proofing)
dist/
build/
.next/
.turbo/
.cache/

# OS thumbnails + misc
Thumbs.db
ehthumbs.db
